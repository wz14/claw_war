# Web 后端
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 异步 HTTP（调微信 iLink Bot 网关）
aiohttp>=3.10.0
certifi>=2024.8.30

# 二维码生成（前端展示）
qrcode[pil]>=7.4.2

# 数据模型
pydantic>=2.9.0

# 加载 .env
python-dotenv>=1.0.1

# LLM 对话与工具调用（默认走 DeepSeek 这种 OpenAI 兼容的便宜模型）
langchain>=0.3.7
langchain-openai>=0.2.8
langgraph>=0.2.50

# 错误监控：未捕获异常 + logger.error 自动上报到 Sentry
# 通过 SENTRY_DSN 环境变量启用；不配就跳过初始化（本地开发友好）
sentry-sdk[fastapi]>=2.20.0
